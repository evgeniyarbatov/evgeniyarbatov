# Roadmap

This repo is a GitHub profile page (`evgeniyarbatov/evgeniyarbatov`), not a project — its only job is to be an accurate, current front door to the rest of the portfolio. A forward-looking roadmap doesn't apply the way it does to a real codebase; the only thing worth tracking here is staying true (see TODO.md for the stale-link check).

If this ever grows beyond a link list — a generated, always-current summary of what's active across the account — that would make it a genuine project again, and worth revisiting: **[private]** and **[private]** already compute exactly that signal (liveliness scores, portfolio reports) and could feed this page automatically instead of it being hand-maintained.
